<div class="container">
    <div class="col-md-3 offset3">
      <% if @item.image.attached? %>
        <%= image_tag @item.image, size: "200x170" %>
      <% else %>
        <%= image_tag 'no_image', size: "200x170" %>
      <% end %>
    </div>
  <div class="col-md-6 offsrt-6">
    <table>
      <tr>
        <td><%= @item.name %></td>
      </tr>
      <tr>
        <td><%= @item.introduction %></td>
      </tr>
      <tr>
        <td><%= @item.price %></td>
        <td><%= @item.introduction %></td>
      </tr>
    </table>
  </div>
</div>